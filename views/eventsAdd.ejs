<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Reminder</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background: linear-gradient(135deg, #7b2ff7, #f107a3);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
    }

    .back-btn {
      position: absolute;
      top: 20px;
      left: 20px;
      background: linear-gradient(45deg, #6a0dad, #8e2de2);
      color: #fff;
      padding: 10px 20px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .back-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
    }

    .container {
      background: #fff;
      border-radius: 20px;
      padding: 40px 50px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
      width: 100%;
      max-width: 450px;
      margin-top: 80px;
      text-align: center;
    }

    h2 {
      font-size: 2rem;
      margin-bottom: 30px;
      background: linear-gradient(45deg, #6a0dad, #8e2de2);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .days-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 10px;
      margin-bottom: 25px;
    }

    .custom-control {
      display: flex;
      align-items: center;
      gap: 5px;
      font-weight: 600;
      cursor: pointer;
    }

    input[type="time"],
    input[type="text"] {
      width: 100%;
      padding: 12px 15px;
      margin-bottom: 25px;
      border-radius: 12px;
      border: 2px solid #dda0ff;
      outline: none;
      font-size: 1rem;
    }

    input[type="time"]:focus,
    input[type="text"]:focus {
      border-color: #6a0dad;
    }

    .purp-btn {
      background: linear-gradient(45deg, #8e2de2, #6a0dad);
      color: #fff;
      padding: 15px 0;
      width: 100%;
      border: none;
      border-radius: 12px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .purp-btn:hover {
      background: linear-gradient(45deg, #6a0dad, #8e2de2);
      transform: scale(1.03);
    }

    .alert {
      padding: 10px 15px;
      border-radius: 12px;
      margin-bottom: 20px;
      font-weight: 600;
    }

    .alert-danger {
      background: #f8d7da;
      color: #721c24;
    }

    .alert-success {
      background: #d4edda;
      color: #155724;
    }
  </style>
</head>

<body>

  <a href="/events" class="back-btn">← Back to Events</a>

  <div class="container">
    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <% if (success && success.length > 0) { %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>

    <h2>Add Reminder</h2>
    <form action="/eventsadd" method="post">
      <div class="days-grid">
        <% const days = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat']; %>
        <% for(let i=0; i<7; i++){ %>
          <label class="custom-control">
            <input type="checkbox" name="<%= i %>" value="<%= i %>">
            <%= days[i] %>
          </label>
        <% } %>
      </div>

      <input type="time" name="time" required>
      <input type="text" name="tag" placeholder="Reminder Message" required maxlength="100">

      <button type="submit" class="purp-btn">Add Reminder</button>
    </form>
  </div>

</body>

</html>
