<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Reminders</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background: linear-gradient(135deg, #7b2ff7, #f107a3);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
    }

    .back-btn {
      position: absolute;
      top: 20px;
      left: 20px;
      background: linear-gradient(45deg, #6a0dad, #8e2de2);
      color: #fff;
      padding: 10px 20px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .back-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
    }

    .header {
      margin-top: 20px;
      font-size: 2rem;
      font-weight: 700;
      color: #fff;
      text-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    .container {
      width: 100%;
      max-width: 900px;
      padding: 20px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      box-sizing: border-box;
      margin-top: 20px;
    }

    .reminder-card {
      background: #fff;
      border-radius: 20px;
      padding: 20px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      align-items: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }

      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .reminder-card:hover {
      transform: translateY(-5px) scale(1.02);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
    }

    .day-time {
      font-size: 1.2rem;
      font-weight: 700;
      margin-bottom: 10px;
      color: #6a0dad;
    }

    .message {
      font-size: 1rem;
      margin-bottom: 15px;
      text-align: center;
      color: #333;
      word-wrap: break-word;
    }

    .countdown {
      font-size: 1.1rem;
      font-weight: 600;
      color: #f107a3;
      text-align: center;
    }

    .no-reminder {
      color: #fff;
      font-size: 1.3rem;
      margin-top: 50px;
      text-align: center;
    }

    /* Add Reminder button below cards */
    .add-btn {
      margin: 40px auto;
      display: block;
      background: linear-gradient(45deg, #f107a3, #7b2ff7);
      color: white;
      border: none;
      border-radius: 12px;
      padding: 15px 25px;
      font-size: 1.2rem;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .add-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4);
    }

    @media (max-width: 600px) {
      .container {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>

  <a href="/home" class="back-btn">← Home</a>
  <div class="header">My Reminders</div>

  <div class="container">
    <% if(result.length === 0){ %>
      <div class="no-reminder"><%= msg %></div>
    <% } else { %>
      <% result.forEach(function(reminder, index){ %>
        <div class="reminder-card" data-time="<%= reminder.time %>">
          <div class="day-time">
            <% reminder.days.forEach(function(d, i){ 
              const days = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat']; %>
              <%= days[d] %><% if(i < reminder.days.length-1){ %>, <% } %>
            <% }) %>
            - <%= reminder.time %>
          </div>
          <div class="message"><%= reminder.tag %></div>
          <div class="countdown">Loading...</div>
        </div>
      <% }); %>
    <% } %>
  </div>

  <!-- Add Reminder Button Below -->
  <a href="/eventsadd" class="add-btn">+ Add Reminder</a>

  <script>
    // Countdown for reminders
    function updateCountdown() {
      const cards = document.querySelectorAll('.reminder-card');
      const now = new Date();

      cards.forEach(card => {
        const timeStr = card.dataset.time; // e.g. "14:30"
        const [hours, minutes] = timeStr.split(':').map(Number);
        const reminderTime = new Date();
        reminderTime.setHours(hours, minutes, 0, 0);

        let diff = reminderTime - now;

        if (diff < 0) {
          card.querySelector('.countdown').innerText = "Time passed";
        } else {
          const h = Math.floor(diff / 3600000);
          const m = Math.floor((diff % 3600000) / 60000);
          const s = Math.floor((diff % 60000) / 1000);
          card.querySelector('.countdown').innerText =
            `${h}h ${m}m ${s}s left`;
        }
      });
    }

    updateCountdown();
    setInterval(updateCountdown, 1000);
  </script>

</body>

</html>