<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alzheimer's Care - Home</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- FontAwesome -->
    <script src="https://kit.fontawesome.com/3808de5cb1.js" crossorigin="anonymous"></script>

    <style>
        /* ---------- Global Styles ---------- */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(145deg, #667eea, #764ba2);
            color: #fff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 40px 20px;
        }

        /* ---------- Container ---------- */
        .container {
            width: 95%;
            max-width: 1400px;
            padding: 2rem;
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(16px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.3);
            animation: fadeIn 1s ease-in-out;
        }

        /* ---------- Profile ---------- */
        #user-dp {
            display: block;
            margin: 0 auto 1rem auto;
            width: 130px;
            height: 130px;
            border-radius: 50%;
            border: 4px solid #fff;
            object-fit: cover;
            box-shadow: 0 6px 20px rgba(0,0,0,0.3);
            transition: transform 0.4s ease, box-shadow 0.4s ease;
        }

        #user-dp:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 30px rgba(0,0,0,0.5);
        }

        /* ---------- Welcome Header ---------- */
        h2.home-head {
            text-align: center;
            font-weight: 700;
            font-size: 2.2rem;
            margin-bottom: 2rem;
            text-shadow: 0 3px 15px rgba(0,0,0,0.3);
            animation: slideInDown 1s ease forwards;
        }

        /* ---------- Grid Cards ---------- */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 1.5rem;
            margin-top: 1rem;
        }

        .home-box {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, rgba(255,255,255,0.1), rgba(255,255,255,0.2));
            border-radius: 20px;
            text-decoration: none;
            color: #fff;
            padding: 1.5rem 1rem;
            transition: all 0.5s ease;
            box-shadow: 0 6px 20px rgba(0,0,0,0.3);
            backdrop-filter: blur(6px);
        }

        .home-box img {
            width: 80px;
            height: 80px;
            margin-bottom: 1rem;
            transition: transform 0.5s ease;
        }

        .home-box h3 {
            font-size: 1.2rem;
            font-weight: 600;
            text-align: center;
            transition: color 0.4s ease;
        }

        .home-box:hover {
            transform: translateY(-10px) scale(1.05);
            background: linear-gradient(135deg, rgba(255,255,255,0.25), rgba(255,255,255,0.35));
            box-shadow: 0 12px 30px rgba(0,0,0,0.4);
        }

        .home-box:hover img {
            transform: scale(1.2) rotate(10deg);
        }

        .home-box:hover h3 {
            color: #764ba2;
        }

        /* ---------- Dashboard Button ---------- */
        .dashboard-btn {
            margin: 3rem auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 220px;
            padding: 1rem 0;
            border-radius: 20px;
            background: linear-gradient(135deg, #ff758c, #ff7eb3);
            box-shadow: 0 10px 30px rgba(0,0,0,0.4);
            text-align: center;
            color: #fff;
            text-decoration: none;
            font-weight: 700;
            transition: all 0.4s ease;
        }

        .dashboard-btn:hover {
            transform: scale(1.08) translateY(-5px);
            box-shadow: 0 12px 36px rgba(0,0,0,0.5);
        }

        /* ---------- Reminders Section ---------- */
        #reminders {
            margin-top: 3rem;
            padding: 1.5rem;
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.12);
            box-shadow: 0 12px 30px rgba(0,0,0,0.35);
            animation: fadeInUp 1s ease forwards;
        }

        #reminders h2 {
            margin-bottom: 1rem;
            font-size: 1.6rem;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }

        table {
            width: 100%;
            color: white;
            border-collapse: collapse;
        }

        th, td {
            text-align: left;
            padding: 0.8rem;
            border-bottom: 1px solid rgba(255,255,255,0.3);
        }

        th {
            font-weight: 600;
        }

        /* ---------- Logout Button ---------- */
        .purp-btn {
            display: block;
            background: linear-gradient(90deg, #743ad5 0%, #d53a9d 100%);
            color: white;
            padding: 1rem 2rem;
            text-decoration: none;
            border-radius: 16px;
            text-align: center;
            width: 220px;
            margin: 2rem auto;
            font-weight: 700;
            transition: all 0.4s ease;
        }

        .purp-btn:hover {
            transform: scale(1.08);
            opacity: 0.95;
        }

        /* ---------- Animations ---------- */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes slideInDown {
            from { opacity: 0; transform: translateY(-40px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* ---------- Responsive ---------- */
        @media (max-width: 700px) {
            .grid-container {
                grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            }
            .dashboard-btn, .purp-btn {
                width: 80%;
            }
        }

    </style>
</head>

<body>
    <div class="container homepage">
        <% if(CurrentUser.DP.data){ %>
            <img id="user-dp" src="data:image/<%=CurrentUser.DP.contentType%>;base64,<%=CurrentUser.DP.data.toString('base64')%>">
        <% } %>

        <h2 class="home-head">Welcome, <%= CurrentUser.name %>!</h2>

        <div class="grid-container">
            <a href="/news" class="home-box">
                <img src="img/news.png" alt="News">
                <h3>News</h3>
            </a>
            <a href="/games" class="home-box">
                <img src="img/games.png" alt="Games">
                <h3>Games</h3>
            </a>
            <a href="/circle" class="home-box">
                <img src="img/circle.png" alt="Circle">
                <h3>Circle</h3>
            </a>
            <a href="/video" class="home-box">
                <img src="img/identify.png" alt="Identify">
                <h3>Identify</h3>
            </a>
            <a href="/events" class="home-box">
                <img src="img/reminder.png" alt="Reminders">
                <h3>Reminders</h3>
            </a>
            <a href="/entertainment" class="home-box">
                <img src="img/entertainment.png" alt="Entertainment">
                <h3>Entertainment</h3>
            </a>
        </div>

        <a href="/dashboard" class="dashboard-btn">
            <img src="img/dashboard.png" width="96" height="96" alt="Dashboard">
            <h3>Dashboard</h3>
        </a>

        <div id="reminders">
            <h2>Reminders</h2>
            <p><%= msg %></p>
            <table>
                <tr>
                    <th>Time</th>
                    <th>Message</th>
                </tr>
                <% for(let i=0; i<myreminders.length; i++){ %>
                    <tr>
                        <td><%= myreminders[i].time %></td>
                        <td><%= myreminders[i].tag %></td>
                    </tr>
                <% } %>
            </table>
        </div>

        <a href="/logout" class="purp-btn">Logout</a>
    </div>
</body>
</html>
