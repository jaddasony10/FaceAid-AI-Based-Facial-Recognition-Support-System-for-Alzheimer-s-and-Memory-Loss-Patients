<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Game Hub</title>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" crossorigin="anonymous">
<link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600;700&display=swap" rel="stylesheet">
<script src="https://kit.fontawesome.com/3808de5cb1.js" crossorigin="anonymous"></script>
<style>
  body {
    font-family: 'Montserrat', sans-serif;
    background: linear-gradient(135deg, #f0f2f5, #dff0fd);
    min-height: 100vh;
    margin: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    overflow-x: hidden;
    animation: bgMove 15s infinite linear;
  }

  @keyframes bgMove {
    0% { background-position: 0 0; }
    50% { background-position: 100% 100%; }
    100% { background-position: 0 0; }
  }

  .header {
    padding: 15px 20px;
    display: flex;
    align-items: center;
    width: 100%;
    position: sticky;
    top: 0;
    background: rgba(255,255,255,0.9);
    backdrop-filter: blur(10px);
    z-index: 10;
    box-shadow: 0 2px 15px rgba(0,0,0,0.1);
  }

  .header img {
    cursor: pointer;
    border-radius: 50%;
    border: 2px solid #6a11cb;
    box-shadow: 0 2px 8px rgba(0,0,0,0.2);
    transition: transform 0.2s ease;
  }

  .header img:hover {
    transform: scale(1.1) rotate(-10deg);
  }

  .title {
    flex: 1;
    text-align: center;
    font-size: 2rem;
    font-weight: 700;
    color: #6a11cb;
    margin: 0 20px;
    animation: fadeInDown 1s ease forwards;
  }

  @keyframes fadeInDown {
    0% { opacity: 0; transform: translateY(-30px); }
    100% { opacity: 1; transform: translateY(0); }
  }

  .main {
    background: #fff;
    padding: 2rem 3rem;
    border-radius: 25px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    width: 90%;
    max-width: 700px;
    margin-top: 40px;
    transform: scale(0.95);
    opacity: 0;
    animation: fadeInUp 0.8s forwards 0.3s;
  }

  @keyframes fadeInUp {
    to { transform: scale(1); opacity: 1; }
  }

  .main h2 {
    font-size: 1.8rem;
    font-weight: 700;
    color: #4a0d66;
    margin-bottom: 2rem;
    text-align: center;
    letter-spacing: 0.5px;
  }

  .buttons {
    display: flex;
    flex-direction: column;
    row-gap: 15px;
    margin-bottom: 3rem;
  }

  .btn-game {
    padding: 15px;
    border-radius: 15px;
    font-size: 1.2rem;
    font-weight: 600;
    text-align: center;
    color: #fff;
    background: linear-gradient(135deg, #6a11cb, #2575fc);
    border: none;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .btn-game i {
    margin-right: 10px;
    transition: transform 0.3s ease;
  }

  .btn-game:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 25px rgba(106,17,203,0.3);
  }

  .btn-game:hover i {
    transform: rotate(360deg);
  }

  #reminders {
    background: linear-gradient(145deg, #f8f0ff, #e6e6ff);
    border-radius: 20px;
    padding: 1.8rem 2rem;
    margin-top: 25px;
    box-shadow: 0 6px 20px rgba(74,13,102,0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  #reminders:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(74,13,102,0.15);
  }

  #reminders h3 {
    color: #6a11cb;
    margin-bottom: 1rem;
    text-align: center;
    letter-spacing: 0.5px;
  }

  table {
    width: 100%;
    border-collapse: collapse;
  }

  th, td {
    padding: 12px 15px;
    border-bottom: 1px solid #ddd;
    font-size: 0.95rem;
    transition: background 0.3s ease;
  }

  th {
    background-color: #fbd7ff;
    color: #7b2e9e;
    font-weight: 600;
  }

  tr:hover td {
    background-color: rgba(106,17,203,0.05);
  }

  @media(max-width: 600px){
    .main { padding: 1.5rem; }
    #reminders { padding: 1rem; }
    .title { font-size: 1.7rem; }
    .btn-game { font-size: 1rem; padding: 12px; border-radius: 12px; }
  }
</style>
</head>
<body>

<div class="header">
  <a href="/home">
    <img src="/img/back.png" alt="Back" height="50px" />
  </a>
  <div class="title">Choose Your Game</div>
</div>

<div class="main">
  <h2>Which game would you like to play?</h2>
  <div class="buttons">
    <a href="/memorygame" class="btn-game"><i class="fas fa-memory"></i> Memory Cards</a>
    <a href="/guesswho" class="btn-game"><i class="fas fa-users"></i> Guess Who</a>
    <a href="/games/SAGE" class="btn-game"><i class="fas fa-puzzle-piece"></i> TEST</a>
  </div>

  <div id="reminders">
    <h3>Reminders</h3>
    <p><%= msg %></p>
    <table>
      <tr>
        <th>Time</th>
        <th>Message</th>
      </tr>
      <% for(var i=0; i<myreminders.length; i++){ %>
      <tr>
        <td><%= myreminders[i].time %></td>
        <td><%= myreminders[i].tag %></td>
      </tr>
      <% } %>
    </table>
  </div>
</div>

</body>
</html>
