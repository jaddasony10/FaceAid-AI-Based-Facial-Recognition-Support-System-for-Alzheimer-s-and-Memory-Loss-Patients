<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Video</title>
    <link rel="stylesheet" href="/stylesheets/app.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/3808de5cb1.js" crossorigin="anonymous"></script>
</head>

<body>
    <div class="header">
        <a href="/entertainment">
            <img src="/img/back.png" alt="Back" height="50">
        </a>
    </div>

    <div class="container">

        <!-- Flash messages -->
        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <% if (success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>

        <h2>Add a YouTube Video</h2>
        <p>Enter the Video ID (the part after `v=` in the YouTube URL)</p>

        <div class="c">
            <form action="/addvideos" method="POST">
                <input type="text" id="videoid" name="videoid" class="form-control" placeholder="Video ID" required>
                <input type="submit" class="purp-btn" value="Add Video">
            </form>
        </div>

    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"
        integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="/js/register.js"></script>
</body>

</html>
